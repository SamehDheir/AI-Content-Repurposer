# Copy to .env.local and adjust. Never commit .env.local.

# The backend origin. Must match CORS_ORIGINS on the backend side, and note
# that this is the BACKEND port (3001) — the frontend itself runs on 3000.
NEXT_PUBLIC_API_URL=http://localhost:3001
