// Twitter needs its own file — it does not fall back to opengraph-image. Same
// card, so the generator is shared rather than duplicated.
export { default, alt, size, contentType } from "./opengraph-image";
